# Convert an NVML error code to an R error

Convert an NVML error code to an R error

## Usage

``` r
nvml_check_status(code)
```

## Arguments

- code:

  Integer NVML return code (0 == success)

## Value

Invisible TRUE on success; otherwise stops with an error
